services:
  nexus:
    build: .
    ports: ["19200:19200"]
    volumes: ["nexus-data:/root/.nexus"]
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    restart: unless-stopped

volumes:
  nexus-data:
